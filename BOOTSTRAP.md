# BOOTSTRAP INSTRUCTION

This repository is the canonical source for your Hermes configuration.

To restore this environment:
1. Read the README.md and MANIFEST.yaml.
2. Ensure you have the required dependencies (see DEPENDENCIES.md).
3. Run the restore script in `scripts/`.
4. Provide any required API keys when prompted.
